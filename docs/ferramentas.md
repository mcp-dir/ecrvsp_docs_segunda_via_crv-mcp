# Ferramentas

ECRVSP Documentos: Segunda Via de CRV expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_docs_segunda_via_crv_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `chassi`, `file_pdf` (opcional), `categoria`, `tipo_restricao`, `cpf` (opcional), `cnpj` (opcional), `endereco_cep`, `endereco_numero`, `endereco_complemento` (opcional), `nome`, `telefone` (opcional), `tipo_placa_dianteira`, `tipo_placa_traseira`, `tipo_placa_traseira_segunda`

ECRVSP Documentos: Segunda Via de CRV, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Documentos: Segunda Via de CRV
```
